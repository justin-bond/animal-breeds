import { styled } from "styled-components";

import { Container } from "../../styles/container";

export const ImageContainer = styled(Container)`
  border-radius: 20px;
  overflow: hidden;
`;

export const BreedDetailsContainer = styled(Container)`
  display: flex;
  justify-content: space-between;
`;
